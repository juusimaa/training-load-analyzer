import type { Day } from "./types";

export const WINDOWS: readonly number[] = [];
export const DEFAULT_WINDOW = 0;

export function windowed(_days: readonly Day[], _n: number): Day[] {
  return [];
}
