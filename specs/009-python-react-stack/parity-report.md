# Parity Report: Re-implementation on an Alternative Stack

**Feature**: 009-python-react-stack | **Spec**: [spec.md](./spec.md) | **Parity contract**: [contracts/parity.md](./contracts/parity.md)

This report is the record 009 US5 asks for. It covers:

- the tolerances allowed between the two stacks;
- every place where the new implementation deliberately differs from the reference;
- the display ties found;
- a map from every functional requirement of features 001–008 to the test that covers it;
- the SC-008 measurements.

The goldens it cites are generated by the reference itself (`dotnet run --project parity/generator`)
and are never edited by hand.

## Tolerances

_Filled in by T109._

## Deviations

_Filled in by T067 and T109._

## Display ties found

Amendment 1(a) allows a displayed figure to differ from the reference only at an exact rounding tie
that the two stacks' arithmetic places on opposite sides. The ties the fixtures reach are listed
here, each with whether the two stacks agree.

| Fixture | Figure | Value (double, round-trip) | 15 significant digits | Reference | New implementation | Agree |
| --- | --- | --- | --- | --- | --- | --- |
| `display-ties` | Fatigue, 2026-09-18 | `1021.2499999999967` | `1021.25` | `1021.3` | `1021.3` | yes |

The `display-ties` fixture was found by a search in the session scratchpad (60 days at 1000.1
points, then 1158.9, then 1022.386). Its loads are exact in both decimal types, so both stacks
compute the same double. Both then round the 15-digit text half away from zero. Python's own
`f"{x:.1f}"` would print `1021.2` here; `tests/dashboard/test_display.py` and the history parity
tests pin the emulation.

No tie has been found where the two stacks' doubles differ.

## Requirement map

_Filled in by T108._

## Measurements

_Filled in by T110._
